{
  "name": "sonic-ai-v2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev -- --hostname 127.0.0.1 --port 3000",
    "dev:frontend": "npm --prefix frontend run dev -- --hostname 127.0.0.1 --port 3000",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend test -- --run",
    "backend": "cd backend && .\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "test:backend": ".\\backend\\.venv\\Scripts\\python.exe -m pytest backend\\tests -q"
  }
}
